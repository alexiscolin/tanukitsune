// Every item is reviewed for its meaning and for its reading, and the two are
// graded by different rules, so the kind travels with the answer from the field
// it was typed in through to the verdict.
export type AnswerKind = 'meaning' | 'reading'
